console.log(restar(2,2))

const list = []
const anotherList = list.concat(100)

const persona = {
    name: 'Daniel',
    username: '@Dalus',
    age: 19,
    isDev: true,
    links: ['https://github.com/Dalus04/Curso-FullStackOpen']
}

const sumar = (a, b) => {
    return a + b
}

function restar (a, b){
    return a - b
}

console.log(sumar(2,2))